---
phase: 01-demo-up-http-lands-on-old
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - proxy/nginx.conf
  - proxy/active-backend.conf
  - compose.yaml
  - scripts/smoke.sh
  - Makefile
autonomous: true
requirements: [ENV-04, HTTP-01, HTTP-02]
mvp_mode: true
walking_skeleton: true

must_haves:
  truths:
    - "`docker compose exec -T client curl -fsS http://app.demo.local:9092/whoami` returns exactly `OLD server-old` — the client uses a real hostname, never `localhost`, and lands on the active backend through nginx (HTTP-01, D-01, D-02)."
    - "`curl -sSL -o /dev/null -w '%{num_redirects}' http://localhost:9092/` returns `0` and `%{url_effective}` equals the requested URL — the proxied request never changes the client's URL (HTTP-02)."
    - "`curl -sSI http://localhost:9092/` includes `X-Backend: OLD`, and that value originated from the backend's own response rather than from any proxy directive (HTTP-01, D-11)."
    - "`docker compose exec -T proxy nginx -V 2>&1 | grep -q -- --with-stream` succeeds, proving the stream module is compiled into the proxy image so Phase 3's port 22 proxying is possible (ENV-04)."
    - "`docker compose logs proxy` contains a line matching `backend=OLD` alongside the requested `$host:$server_port` — the log format exposes which upstream served each request (Phase 2 EVID-01 precondition)."
    - "`proxy/active-backend.conf` is a self-contained file of no more than five lines whose only meaningful token is `old`, so the Phase 2 flip is a one-word edit the audience can read in full on screen (D-12)."
    - "The proxy starts cleanly from a cold `docker compose up` on every attempt — `depends_on: condition: service_healthy` on both backends prevents the parse-time upstream resolution failure that otherwise makes startup intermittent (RESEARCH Pitfall 4)."
    - "With a deliberately invalid value in `proxy/active-backend.conf`, `curl http://localhost:9092/` returns 503 with a body naming the invalid value and the two legal values — not a bare 502 (RESEARCH Pitfall 3)."
    - "`docker compose exec -T proxy nginx -t` exits zero, and `docker compose exec -T proxy nginx -s reload` is the mechanism by which config changes are picked up — no container restart is required (D-14)."

  artifacts:
    - proxy/nginx.conf
    - proxy/active-backend.conf

  key_links:
    - "`proxy/active-backend.conf` defines `$active_backend` via a `map`; `proxy/nginx.conf` declares `upstream old` and `upstream new` and consumes the variable as `proxy_pass http://$active_backend;`. The selector, not the target, lives in the shared file — this is what lets Phase 3's `stream` block include THE SAME FILE without restructuring (D-12 + D-13, RESEARCH Pattern 1)."
    - "backend `add_header X-Backend` -> proxy `$upstream_http_x_backend` -> the `demo` log format's `backend=` field. D-11 and the log format are one decision; Phase 2's EVID-01 consumes this exact field (RESEARCH Pattern 6)."
    - "compose `aliases: [app.demo.local]` on the proxy service -> Docker embedded DNS -> the `client` container resolves the real hostname straight to the proxy container, preserving the client's real source IP in the proxy log (D-01, D-02, RESEARCH Alternatives Considered)."
    - "compose `depends_on: {server-old: {condition: service_healthy}, server-new: {condition: service_healthy}}` -> the healthchecks defined in plan 01-01. Load-bearing, not decorative: nginx resolves upstream hostnames at config-parse time and aborts if DNS cannot answer."
    - "`$backend_is_valid` map in `nginx.conf` (NOT in `active-backend.conf`) -> a `return 503` guard. Keeps the file the audience reads at three lines while turning a typo'd flip from a mystery 502 into a legible message."

  prohibitions:
    - requirement_id: HTTP-01
      category: transparency
      statement: "MUST NOT synthesize, hardcode, or override the backend identity anywhere in the proxy config or the test path. Every OLD/NEW claim the audience sees — the page banner, the `X-Backend` header, and the `backend=` log field — must originate from the backend container that actually served the request. A proxy-set `add_header X-Backend`, a hardcoded value in `smoke.sh`, or a log field derived from the configured upstream name rather than the observed response would make the demo assert something it has not proven."
---

## Phase Goal

**As a** presenter, **I want to** send a request to `app.demo.local:9092` and have nginx transparently land it on `server-old` with the URL unchanged, **so that** the audience sees the migration endpoint working before I flip it.

<objective>
Introduce nginx into the story. The proxy service joins the same `docker compose up`, listens on 9092, and forwards to whichever backend `proxy/active-backend.conf` selects — which in Phase 1 is always `old`. The client container resolves `app.demo.local` through Docker DNS and reaches the proxy directly; the host browser reaches the same name through the D-03 `/etc/hosts` entry.

This plan also settles three things Phase 1 owns on behalf of later phases: the shape of the flip include (D-12 + D-13), the access log format that Phase 2's EVID-01 consumes, and the `$backend_is_valid` guard that turns a typo'd flip into a legible 503 instead of a mid-stage 502.

Purpose: this is the walking skeleton's spine — one real HTTP request traversing nginx, landing on an identified backend, observable in the log.
Output: `proxy/nginx.conf`, `proxy/active-backend.conf`, an extended `compose.yaml` and `scripts/smoke.sh`, and a `make reload` target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-up-http-lands-on-old/01-CONTEXT.md
@.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md
@.planning/phases/01-demo-up-http-lands-on-old/01-VALIDATION.md
@.planning/phases/01-demo-up-http-lands-on-old/SKELETON.md
@.planning/phases/01-demo-up-http-lands-on-old/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The proxy config — flip include, transparent 9092, log format, validity guard</name>

  <read_first>
    - `.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md` § "Pattern 1: The dual-context flip include (D-12 + D-13)" — contains the verified `active-backend.conf` and the Phase 1 form of `nginx.conf`, including the commented Phase 3 stream block that must stay commented.
    - `.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md` § "Pattern 6: The log format" — the verified `log_format demo` line and the rationale for each field.
    - `.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md` § "Pitfall 3" — contains the verified `$backend_is_valid` map and 503 guard.
    - `.planning/phases/01-demo-up-http-lands-on-old/01-CONTEXT.md` § "nginx config shape" — D-12, D-13, D-14, D-15.
    - `.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md` § "Anti-Patterns to Avoid" — specifically the trailing-slash `proxy_pass` hazard and why no stream block ships in Phase 1.
  </read_first>

  <files>proxy/active-backend.conf, proxy/nginx.conf</files>

  <behavior>
    - `nginx -t` against this config with both backends resolvable exits zero.
    - A request to :9092 with `active-backend.conf` selecting `old` reaches `server-old:80` with the original request URI intact.
    - The access log line for that request contains the requested host and port, the upstream address, and `backend=OLD`.
    - A request to :9092 with an unrecognised selector value returns 503 with a legible message.
  </behavior>

  <action>
Create `proxy/active-backend.conf` as the single file the presenter edits. It contains a `map` keyed on `$server_port` producing `$active_backend`, with a `default` of `old`, plus a two-line comment naming it as THE ONLY FILE TO EDIT and stating that changing `old` to `new` performs the cutover. Keep the whole file at five lines or fewer — it is read aloud on stage.

The include must contain a `map`, NOT an `upstream` block. An `upstream` cannot be shared between the `http` and `stream` contexts: the two contexts keep separate upstream namespaces and the backend ports differ (80 vs 22). Putting the SELECTOR rather than the TARGET in the shared file is what satisfies D-12 and D-13 simultaneously, and is the design RESEARCH verified working from both contexts.

Create `proxy/nginx.conf` as the main config, Phase 1 form. It needs:
- `worker_processes 1;` and a minimal `events` block.
- An `http` block including `/etc/nginx/mime.types`.
- A `log_format` named `demo` carrying, in order: `$remote_addr`, an arrow, `$host` and `$server_port`, the quoted `$request`, `$status`, `upstream=$upstream_addr`, `backend=$upstream_http_x_backend`, `rt=$request_time`, and `urt=$upstream_response_time`. Copy this line verbatim from RESEARCH Pattern 6 — Phase 2's EVID-01 is written against these exact field names and changing them later means re-rehearsing the flip-moment log tail.
- `access_log /dev/stdout demo;` and `error_log /dev/stderr notice;`. Logging to stdout rather than a file is what makes `docker compose logs -f proxy` the live tail EVID-01 asks for, with no volume mount.
- `upstream old` pointing at `server-old:80` and `upstream new` pointing at `server-new:80`, declared BEFORE the include.
- `include /etc/nginx/demo/active-backend.conf;` — mount the DIRECTORY, not the single file, per RESEARCH Pitfall 10's robustness recommendation, so an editor that replaces the file's inode on the presenter's machine cannot leave the container holding a stale inode.
- A `map $active_backend $backend_is_valid` block with `default 0`, `old 1`, `new 1`. This lives in `nginx.conf`, NOT in `active-backend.conf`, so the file the audience reads during the Phase 2 flip stays three lines.
- A `server` block listening on 9092. Inside it, an `if ($backend_is_valid = 0)` guard returning 503 with a plain-text body naming the offending `$active_backend` value and stating the two legal values. Then `location /` with `proxy_pass http://$active_backend;` — with NO trailing slash after the variable, because a trailing slash makes nginx substitute the URI instead of passing it through. Set `proxy_set_header` for `Host` from `$host`, `X-Real-IP` from `$remote_addr`, and `X-Forwarded-For` from `$proxy_add_x_forwarded_for`.

The proxy must never emit an identity header of its own. `proxy_pass` forwards the backend's own response headers to the client verbatim, and the same value reaches the log as `$upstream_http_x_backend`, so no response-header-setting directive belongs in this config at all. A proxy-emitted identity header would make the demo assert something it did not observe.

<!-- planner-discipline-allow: add_header -->
  

Do NOT add a `stream` block, and do NOT create any file with a `.stream-template` suffix (D-15). The nginx image's entrypoint auto-appends its own stream include when it finds such a file; shipping none keeps that machinery dormant until Phase 3 writes the block deliberately.

Do NOT add a 9093 listener in this task — the redirect contrast is plan 01-03.
  </action>

  <acceptance_criteria>
    - `test -f proxy/nginx.conf && test -f proxy/active-backend.conf` succeeds.
    - `awk 'END{print NR}' proxy/active-backend.conf` returns 5 or fewer (D-12 — the file is readable in full on screen).
    - `grep -q 'map \$server_port \$active_backend' proxy/active-backend.conf` succeeds and `grep -q 'default old' proxy/active-backend.conf` succeeds.
    - `grep -q 'upstream old' proxy/nginx.conf && grep -q 'upstream new' proxy/nginx.conf` succeeds.
    - `grep -q 'backend=\$upstream_http_x_backend' proxy/nginx.conf` succeeds (the field Phase 2 EVID-01 consumes).
    - `grep -q 'access_log /dev/stdout demo' proxy/nginx.conf` succeeds.
    - `grep -q 'backend_is_valid' proxy/nginx.conf` succeeds (RESEARCH Pitfall 3 guard present).
    - `grep -q 'listen 9092' proxy/nginx.conf` succeeds.
    - `docker run --rm -v "$PWD/proxy:/etc/nginx/demo:ro" -v "$PWD/proxy/nginx.conf:/etc/nginx/nginx.conf:ro" --entrypoint nginx nginx:1.30-alpine -t` reports either a successful test or specifically the `host not found in upstream` error — any OTHER syntax error is a failure. (Upstream resolution legitimately fails outside the compose network; syntax must still be valid.)
    - `proxy_pass http://$active_backend;` appears with no trailing slash after the variable name.
  </acceptance_criteria>

  <verify>
    <automated>test $(awk 'END{print NR}' proxy/active-backend.conf) -le 5 && grep -q 'map \$server_port \$active_backend' proxy/active-backend.conf && grep -q 'default old' proxy/active-backend.conf && grep -q 'upstream old' proxy/nginx.conf && grep -q 'upstream new' proxy/nginx.conf && grep -q 'backend=\$upstream_http_x_backend' proxy/nginx.conf && grep -q 'backend_is_valid' proxy/nginx.conf && grep -q 'listen 9092' proxy/nginx.conf</automated>
  </verify>

  <done>`proxy/active-backend.conf` is a five-line-or-fewer `map` include whose only meaningful token is `old`. `proxy/nginx.conf` declares both upstreams, consumes the selector via a variable `proxy_pass`, logs the serving backend by name, and guards against an invalid selector with a 503. No stream block ships.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proxy joins the stack — 9092 lands on OLD through a real hostname</name>

  <read_first>
    - `.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md` § "Pattern 5: Compose orchestration with real health gating" — the verified `proxy` service definition including the network alias and the health-gated `depends_on`.
    - `.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md` § "Pitfall 4" — why the health gate is load-bearing.
    - `.planning/phases/01-demo-up-http-lands-on-old/01-RESEARCH.md` § "Pitfall 6" — why source IP is NOT the HTTP-02 evidence on macOS, and what is.
    - `.planning/phases/01-demo-up-http-lands-on-old/01-VALIDATION.md` § "Per-Task Verification Map" — rows for ENV-04, HTTP-01, HTTP-02, the `X-Backend` row, and the Phase 2 log-format precondition row. These are the exact commands to put in the `proxy` section of `scripts/smoke.sh`.
    - `compose.yaml` and `scripts/smoke.sh` as created in plan 01-01.
  </read_first>

  <files>compose.yaml, scripts/smoke.sh, Makefile</files>

  <behavior>
    - `docker compose up -d --wait` from cold brings the proxy up only after both backends are healthy, on every attempt.
    - `curl http://localhost:9092/whoami` returns `OLD server-old`.
    - `docker compose exec client curl http://app.demo.local:9092/whoami` returns the same, and the proxy log records the client's real container IP and the hostname it asked for.
    - `sh scripts/smoke.sh proxy` exits zero.
  </behavior>

  <action>
Add a `proxy` service to `compose.yaml` using `image: nginx:1.30-alpine` — the same pinned stable minor as the backend base, so there is one nginx version to explain on stage. Mount `./proxy` read-only at `/etc/nginx/demo` (the DIRECTORY mount, matching the include path chosen in Task 1) and mount `./proxy/nginx.conf` read-only at `/etc/nginx/nginx.conf`. Publish `127.0.0.1:9092:9092`. Under `networks.default.aliases`, add `app.demo.local` so the client container resolves the real hostname straight to the proxy container through Docker's embedded DNS.

Do not use `extra_hosts` with `host-gateway` for this. RESEARCH tested both: `host-gateway` resolved to an IPv6 address and routed the client out to the host and back in through the published port, which both muddies the conceptual path and hides the client's real IP from the proxy log. The network alias resolves directly and preserves the source IP.

Add `depends_on` with `condition: service_healthy` for BOTH `server-old` and `server-new`. This is load-bearing: nginx resolves `upstream ... server <name>` at config-parse time, not per request, and aborts hard with `host not found in upstream` if Docker DNS cannot answer yet. Without the gate, `docker compose up` fails intermittently — a demo-day failure mode RESEARCH reproduced directly.

Add `depends_on: [proxy]` to the existing `client` service.

The `:ro` mounts are safe: the presenter edits the file on the HOST and the container sees the change immediately. Read-only only prevents writes from inside the container.

Replace the `proxy` stub section in `scripts/smoke.sh` with real assertions, taking the commands verbatim from `01-VALIDATION.md`'s Per-Task Verification Map:
- ENV-04: `docker compose exec -T proxy nginx -V 2>&1` output contains `--with-stream`.
- HTTP-01: `curl -fsS http://localhost:9092/whoami` matches anchored `^OLD server-old$`.
- HTTP-01 via the real hostname: `docker compose exec -T client curl -fsS http://app.demo.local:9092/whoami` matches the same anchored pattern.
- HTTP-02: `curl -sSL -o /dev/null -w '%{num_redirects}'` against `http://localhost:9092/` equals `0`.
- HTTP-02 URL stability: `curl -sSL -o /dev/null -w '%{url_effective}'` against `http://localhost:9092/whoami` equals that same URL string.
- D-11 through the proxy: `curl -sSI http://localhost:9092/` contains a case-insensitive `X-Backend: OLD` line.
- Phase 2 EVID-01 precondition: after issuing at least one request, `docker compose logs proxy` contains `backend=OLD`.
- Pitfall 3 guard: temporarily write an invalid selector into `proxy/active-backend.conf`, run `nginx -t` (expect zero) and `nginx -s reload`, assert `curl -o /dev/null -w '%{http_code}'` on 9092 returns `503`, then restore the file to `default old`, reload again, and assert 9092 returns `200`. Restore the file in a trap so an interrupted run does not leave the rig broken.

Add a `reload` target to the `Makefile` per RESEARCH Pitfall 3's recommended pattern: `nginx -t` first, then `nginx -s reload`, then a smoke request against 9092. This establishes the test-then-reload-then-verify discipline that Phase 2's `make flip` inherits. Use `nginx -s reload` — never `docker compose restart proxy`, which contradicts D-14 and throws away the zero-downtime point.
  </action>

  <acceptance_criteria>
    - `docker compose config --quiet` exits zero.
    - `docker compose down -v && docker compose up -d --build --wait` exits zero from cold, repeated three times consecutively without a single `host not found in upstream` failure (RESEARCH Pitfall 4 regression check).
    - `docker compose exec -T proxy nginx -V 2>&1 | grep -q -- --with-stream` succeeds (ENV-04).
    - `curl -fsS http://localhost:9092/whoami` output equals `OLD server-old` (anchored, HTTP-01).
    - `docker compose exec -T client curl -fsS http://app.demo.local:9092/whoami` output equals `OLD server-old` (HTTP-01 via the real hostname, D-01/D-02).
    - `curl -sSL -o /dev/null -w '%{num_redirects}' http://localhost:9092/` outputs `0` (HTTP-02).
    - `test "$(curl -sSL -o /dev/null -w '%{url_effective}' http://localhost:9092/whoami)" = "http://localhost:9092/whoami"` succeeds (HTTP-02).
    - `curl -sSI http://localhost:9092/ | grep -qi '^X-Backend: OLD'` succeeds.
    - Identity-honesty check: `grep -v '^[[:space:]]*#' proxy/nginx.conf | grep -ci 'add_header'` returns `0` — no response-header-setting directive exists in any non-comment line of the proxy config, proving the `X-Backend` value observed above was produced by the backend rather than the proxy. Comments are filtered so the file's own explanatory prose cannot self-invalidate the gate.
    - `docker compose logs proxy | grep -q 'backend=OLD'` succeeds (Phase 2 EVID-01 precondition).
    - `docker compose logs proxy | grep -q 'app.demo.local:9092'` succeeds after a client-container request — the log records the hostname the client asked for, which is the HTTP-02 evidence (source IP is NOT, per Pitfall 6).
    - Invalid-selector guard: with an unrecognised value in `active-backend.conf`, `nginx -t` exits zero, `nginx -s reload` succeeds, and 9092 returns HTTP `503` with a body naming the invalid value. After restoring `default old` and reloading, 9092 returns `200`.
    - `docker compose ps --format '{{.Ports}}'` lists 9090, 9091 and 9092 as published host ports, all bound to `127.0.0.1`, and lists no port 22 binding.
    - `sh scripts/smoke.sh proxy` exits ZERO. `sh scripts/smoke.sh backends` still exits zero (no regression).
    - `make -n reload` prints a recipe containing `nginx -t` before `nginx -s reload`, and contains no `docker compose restart`.
  </acceptance_criteria>

  <verify>
    <automated>docker compose config --quiet && docker compose down -v && docker compose up -d --build --wait && sh scripts/smoke.sh backends && sh scripts/smoke.sh proxy && docker compose exec -T proxy nginx -V 2>&1 | grep -q -- --with-stream && test "$(curl -sSL -o /dev/null -w '%{url_effective}' http://localhost:9092/whoami)" = "http://localhost:9092/whoami" && make -n reload | grep -q 'nginx -t'</automated>
  </verify>

  <done>The proxy joins the same single `docker compose up`, reliably starting only after both backends are healthy. A request to `app.demo.local:9092` from either the client container or the host lands on `server-old` with the URL unchanged and the identity provably supplied by the backend. The access log names the serving backend. An invalid flip value produces a legible 503 rather than a mystery 502.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host loopback -> published port 9092 | Untrusted local requests enter the proxy |
| Client container -> proxy container (Docker DNS `app.demo.local`) | In-network request path |
| Proxy -> backend containers (`proxy_pass`) | The proxy forwards attacker-influenced headers to the backends |
| Presenter's editor -> bind-mounted `proxy/` -> live nginx config | A host-side file edit changes running proxy behaviour on reload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Denial of Service | `active-backend.conf` selector with a typo'd value | high | mitigate | `$backend_is_valid` map plus a `return 503` guard in `nginx.conf`. Asserted end-to-end in Task 2 by writing an invalid value, reloading, and confirming a 503 with a legible body. Without this the failure is a bare 502 mid-cutover. |
| T-01-08 | Denial of Service | Proxy aborts at config-parse time when an upstream hostname does not resolve | high | mitigate | `depends_on: {server-old: {condition: service_healthy}, server-new: {condition: service_healthy}}`. Asserted by three consecutive cold `down -v` / `up --wait` cycles. |
| T-01-09 | Spoofing | Proxy asserting a backend identity it did not observe | medium | mitigate | No `add_header` directive exists anywhere in `proxy/nginx.conf`; the identity reaches both the client and the log solely as the upstream's own response header via `$upstream_http_x_backend`. Asserted by `grep -c 'add_header' proxy/nginx.conf` returning 0. |
| T-01-10 | Information Disclosure | Port 9092 bound on all host interfaces, exposing the demo on untrusted wifi | medium | mitigate | Publish as `127.0.0.1:9092:9092`. D-03 maps `app.demo.local` to `127.0.0.1`, so loopback binding costs the demo nothing. |
| T-01-11 | Tampering | Attacker-controlled `Host` header propagated to the backends via `proxy_set_header Host $host` | low | accept | The backends serve static content and fixed `return` responses with no host-based routing, so a forged `Host` reaches no interpreter and changes no behaviour. Preserving `$host` is required for the HTTP-02 log evidence. Loopback-only binding bounds the exposure. |
| T-01-12 | Tampering | Bind-mounted config modified from inside the proxy container | low | mitigate | Both mounts are `:ro`. The presenter edits on the host, which the read-only flag does not prevent. |
| T-01-SC | Tampering | Package-manager installs | low | accept | This plan installs nothing. `nginx:1.30-alpine` is pulled by pinned tag; RESEARCH § Package Legitimacy Audit approved it as a Docker Official Image with zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts. |

ASVS Level 1; blocking threshold `high`. Both `high` threats (T-01-07, T-01-08) are dispositioned `mitigate` with asserted implementations, so nothing blocks this plan.
</threat_model>

<flagged_assumptions>
## Flagged Assumptions (probe-surfaced, unresolved)

The spec-less edge probe returned these rows as `unclassified`. Per protocol they are NOT auto-backstopped and NOT dismissed.

1. **ENV-04 (`unclassified`)** — The probe could not classify "nginx container includes the `stream` module so TCP proxying on port 22 works". Assumption carried: the requirement is satisfied by proving the module is COMPILED IN (`nginx -V` shows `--with-stream`), not by shipping a working stream block — which is exactly what D-15 locks. RESEARCH additionally established that ENV-04 imposes no real constraint on image choice, since every official nginx variant ships the flag. If the phase verifier reads ENV-04 as requiring a functioning port-22 proxy, that is Phase 3 work and this phase would need re-scoping.
2. **HTTP-01 (`unclassified`)** — The probe could not classify "client can reach the active backend by connecting to nginx on port 9092 over plain HTTP". Assumption carried: "the client" means both the `client` container (via Docker DNS on `app.demo.local`) and the host browser/curl (via the D-03 `/etc/hosts` entry), and BOTH paths are asserted. Unverified sub-assumption: no TLS variant is expected — PROJECT.md places HTTPS out of scope.
3. **HTTP-02 (`unclassified`)** — The probe could not classify "nginx forwards port 9092 traffic transparently — the client's address and port never change". Assumption carried, and this one is load-bearing: "the client's address and port" is read as the client's REQUESTED URL (scheme, host, port, path), NOT its source IP. RESEARCH Pitfall 6 verified that on macOS Docker Desktop a host-originated request arrives SNAT'd as `192.168.65.1`, so a source-IP reading of HTTP-02 would be unsatisfiable on the target machine. The URL reading is asserted via `%{url_effective}` and `%{num_redirects}`; the log's `$host:$server_port` field is the corroborating evidence.

Probe accounting for this plan: 3 items surfaced, 0 authored as `must_haves.truths` from the probe, 3 surfaced as flagged assumptions above. 0 + 3 = 3. No silent drops.
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts This Plan Produces

Every symbol below is NEW in this plan (or is a new symbol added to a file created in plan 01-01). Source-grounding drift verification must exclude these.

**Files**
- `proxy/nginx.conf`
- `proxy/active-backend.conf`

**Compose service names added**
- `proxy`

**Compose network aliases**
- `app.demo.local` (on the `proxy` service)

**nginx variables and maps introduced**
- `$active_backend` (defined by the `map` in `active-backend.conf`)
- `$backend_is_valid` (defined by the `map` in `nginx.conf`)

**nginx upstream group names**
- `old` (-> `server-old:80`)
- `new` (-> `server-new:80`)

**nginx log_format name**
- `demo` (fields: `$remote_addr`, `$host`, `$server_port`, `$request`, `$status`, `upstream=`, `backend=`, `rt=`, `urt=`)

**Makefile targets defined here**
- `reload`

**smoke.sh sections implemented here**
- `proxy` (replaces the plan 01-01 stub)

**In-container paths introduced**
- `/etc/nginx/demo/` (directory bind mount of the host `proxy/` directory)
- `/etc/nginx/demo/active-backend.conf`

**Published host ports added**
- `127.0.0.1:9092` -> proxy `:9092`
</artifacts_this_phase_produces>

<verification>
Run from a cold state, in order:

1. `docker compose down -v && docker compose up -d --build --wait` — exits zero. Repeat three times; no `host not found in upstream` failure on any attempt.
2. `docker compose exec -T proxy nginx -V 2>&1 | grep -- --with-stream` — prints the flag. ENV-04.
3. `curl -sS -i http://localhost:9092/whoami` — 200, `X-Backend: OLD`, body `OLD server-old`. HTTP-01.
4. `curl -sSL -o /dev/null -w 'final=%{url_effective} redirects=%{num_redirects}\n' http://localhost:9092/whoami` — `redirects=0` and the final URL equals the requested URL. HTTP-02.
5. `docker compose exec client curl -sS http://app.demo.local:9092/whoami` — `OLD server-old`. D-01/D-02.
6. `docker compose logs proxy | tail -5` — lines show `app.demo.local:9092` and `backend=OLD`. Phase 2 EVID-01 precondition.
7. `sh scripts/smoke.sh backends && sh scripts/smoke.sh proxy` — both exit zero.
8. `cat proxy/active-backend.conf` — five lines or fewer, one meaningful token. D-12.
</verification>

<success_criteria>
- `nginx -V` inside the proxy container shows `--with-stream` (ENV-04).
- A request to `app.demo.local:9092` from the client container and to `localhost:9092` from the host both land on `server-old` (HTTP-01).
- The proxied request produces zero redirects and an unchanged effective URL (HTTP-02).
- The access log names the serving backend as `backend=OLD` and records the requested `app.demo.local:9092` (Phase 2 EVID-01 precondition).
- `proxy/active-backend.conf` is a five-line-or-fewer `map` include reusable from a `stream` context without restructuring (D-12, D-13).
- An invalid selector value produces a 503 with a legible message rather than a 502 (RESEARCH Pitfall 3).
- Three consecutive cold starts succeed with no upstream-resolution failure (RESEARCH Pitfall 4).
- `sh scripts/smoke.sh backends` and `sh scripts/smoke.sh proxy` both exit zero.
</success_criteria>

<output>
Create `.planning/phases/01-demo-up-http-lands-on-old/01-02-SUMMARY.md` when done.
</output>
